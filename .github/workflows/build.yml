name: Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.24'
          cache: true
      - name: Build (non-Windows)
        if: runner.os != 'Windows'
        run: go build -o ariadne-${{ runner.os }} ./cmd/ariadne
      - name: Build (Windows)
        if: runner.os == 'Windows'
        run: go build -o ariadne-${{ runner.os }}.exe ./cmd/ariadne
      - name: Upload artifact (non-Windows)
        if: runner.os != 'Windows'
        uses: actions/upload-artifact@v4
        with:
          name: ariadne-${{ runner.os }}
          path: ariadne-${{ runner.os }}
      - name: Upload artifact (Windows)
        if: runner.os == 'Windows'
        uses: actions/upload-artifact@v4
        with:
          name: ariadne-${{ runner.os }}
          path: ariadne-${{ runner.os }}.exe
